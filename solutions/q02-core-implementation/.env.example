# Copy to .env and fill in for live OpenAI calls (Chat Completions, model gpt-4o-mini).
OPENAI_API_KEY=

# Optional: force mock LLM (no API calls) even if OPENAI_API_KEY is set.
# LLM_MODE=mock
# MOCK_LLM=1
